/*
    - single and double 

    - node class


    - node -> node -> node -> node -> null
        head                   tail
    

    looped list is called - CYCLE
*/

class ListNode {
  constructor(value, node) {
    this.value = value;
    this.next = node;
  }
}
